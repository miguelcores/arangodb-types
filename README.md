# ArangoDB types and utilities

This repo contains two Rust libraries:

- **arangodb-models**: a set of macros to automatically generate DB models.
- **arangodb-types**: helper types and utilities to use in DB.